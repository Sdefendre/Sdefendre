# GitHub credibility plan

Prepared from the live `Sdefendre` account on 2026-08-13. This file is a review plan only; no GitHub settings or repository metadata have been changed.

## Exact six-repository pin recommendation

GitHub currently reports zero pinned repositories. Pin these six, in this order:

1. `freevoicetranscribe` — strongest working product and engineering depth
2. `traces-app` — ambitious TypeScript/Electron/3D product work
3. `lingbot-vision-pro` — differentiated Swift/RealityKit spatial work
4. `AirlineFlightSchedule` — strongest public academic Java project, with tests and recent engineering improvements
5. `cs110-assignment-5-atomic-nature-of-matter` — strongest public academic Python project
6. `steve-defendre-portfolio` — verified live portfolio and contact destination

`Wealthwise` is intentionally absent: it is private and cannot serve as inspectable public proof. Reconsider it only after a separate code, history, data, and secrets review supports making it public.

## Metadata priorities

Exact wording should be reviewed before applying it through GitHub.

| Repository | Description | Topics | Homepage | License |
| --- | --- | --- | --- | --- |
| `freevoicetranscribe` | Keep current description | Add `macos`, `dictation`, `speech-to-text`, `whisper`, `mlx`, `apple-silicon`, `privacy`, `python` | Replace generic studio URL with a verified product page when one exists; otherwise use the repository | MIT already detected |
| `traces-app` | “Local-first knowledge workspace with a 3D graph, Markdown editor, and multi-provider AI assistant.” | Add `electron`, `nextjs`, `typescript`, `knowledge-management`, `local-first`, `threejs`, `markdown`, `ollama` | Use the verified portfolio projects page or leave blank until a dedicated page exists | MIT already detected |
| `lingbot-vision-pro` | Keep current description | Keep current strong topic set | Keep verified project page | Apache-2.0 already detected |
| `AirlineFlightSchedule` | “Academic Java flight-scheduling system with searchable itineraries, safer input handling, and JUnit tests.” | Add `java`, `junit`, `data-structures`, `coursework`, `command-line` | Leave blank | Decide deliberately; do not license coursework without confirming course policy and ownership |
| `cs110-assignment-5-atomic-nature-of-matter` | “Python coursework using image luminance, connected components, and particle tracking.” | Add `python`, `depth-first-search`, `image-processing`, `algorithms`, `coursework` | Leave blank | Same coursework policy check |
| `steve-defendre-portfolio` | “Steve Defendre’s portfolio: technical products, client work, and software-engineering case studies.” | Add `portfolio`, `nextjs`, `typescript`, `react`, `software-engineering` | Keep verified live site | Decide whether source is MIT or all-rights-reserved; add a file only after that choice |

## Remaining public repository audit

| Repositories | Recommended metadata action | Archive posture |
| --- | --- | --- |
| `Sdefendre` | Replace generic description with “GitHub profile for Steve Defendre — Computer Science student and technical product engineer.” Add `profile-readme`, `computer-science`, `software-engineering`, `portfolio`; use the verified portfolio homepage | Keep active |
| `cs110-assignment-4-rsa-cryptosystem`, `cs110-assignment-3-mozart-waltz-generator`, `cs110-assignment-2-control-flow-programs` | Add one-sentence assignment descriptions plus `python`, `computer-science`, `coursework` and assignment-specific topics; no homepage; use the coursework license policy above | Keep as academic record while studies are current |
| `voice-butler-skill` | Keep description; add `shell`, `text-to-speech`, `codex`, `claude-code`, `developer-tools`; leave homepage blank; decide license before inviting reuse | Keep if maintained; otherwise archive after documenting supported hosts |
| `SocialMediaManagerAgent` | Add a truthful feature/status description and `nextjs`, `typescript`, `social-media`, `ai-agent`; keep the verified live homepage; decide license | Keep only if the live demo remains maintained |
| `voicecast-ai` | Keep description; add `python`, `text-to-speech`, `podcast`, `generative-ai`; leave homepage blank unless a demo exists | Keep; MIT already detected |
| `spendwise` | Add a truthful README-derived description and topics only after a fresh source review; no homepage currently verified; decide license | Review against WealthWise for duplication, then archive if superseded |
| `DateToWords`, `inputValidatorApp`, `DiceRoller`, `diceroll`, `PrimeNumberCalculator`, `Real-Estate-Property-Value-Estimator`, `Area-Perimeter-Calculator`, `Console-Calculator-` | Add brief academic descriptions and language/coursework topics only if retained; no homepages; do not bulk-license coursework | Archive candidates after confirming they are preserved in the portfolio/academic record; prioritize the duplicate dice projects and smallest single-exercise repositories |
| `grok-app`, `lingbot-map` | Clearly retain upstream attribution and fork status; avoid presenting them as original work. Existing upstream licenses are detected | Keep only when needed for active upstream work; otherwise GitHub fork status already limits profile noise |

## License rule

Do not bulk-add MIT files. Licenses are already detected for `freevoicetranscribe`, `traces-app`, `lingbot-vision-pro`, `voicecast-ai`, and both public forks. For original apps without a license, choose open-source or proprietary intent first. For coursework, confirm the institution’s policy and ownership before adding any reuse grant.

## External approval checklist

- Apply the six pins in the stated order.
- Update the profile repository description, topics, and homepage.
- Apply the six priority repositories’ descriptions, topics, and homepages.
- Apply the remaining repository metadata only after checking each proposed wording against its README.
- Make individual license decisions; never bulk-license.
- Archive only the named candidates after one final source/history/deployment review.
- Keep `Wealthwise` private unless a separate publication review approves changing visibility.
